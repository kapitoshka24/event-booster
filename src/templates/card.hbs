<div class="card">
    <div class="image-container">
        <img class="event-image" src={{#with images}}{{[7].url}}{{/with}} alt="temp">
    </div>
    <h2 class="event-title">{{name}}</h2>
    <p class="event-data">{{dates.start.localDate}}</p>
    <p class="event-location"> {{#with _embedded.venues}}{{[0].name}}{{/with}}</p>
</div>